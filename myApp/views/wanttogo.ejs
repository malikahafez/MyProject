<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        body {
            background-image: url("background3.jpg");
            background-repeat: no-repeat;
            background-size: cover;
            color: white;
            text-align: center;
        }
        h1 {
            -webkit-text-stroke: 1px blue;
        }
        ul {
            list-style: none;
            padding: 0;
        }
        li {
            font-size: 1.5em;
            margin: 10px 0;
        }
        .btn {
            margin: 10px;
            padding: 10px 20px;
            font-size: 1em;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .btn-primary {
            background-color: blue;
            color: white;
        }
        .btn-danger {
            background-color: red;
            color: white;
        }
    </style>
    <meta charset="UTF-8">
    <title>Want-to-Go List</title>
</head>
<body>
    <h1>Your Want-to-Go List</h1>
    <% if (list.length > 0) { %>
        <ul>
            <% list.forEach(destination => { %>
                <li><%= destination %></li>
            <% }) %>
        </ul>
        <form action="/wanttogo/clear" method="POST">
            <button class="btn btn-danger" type="submit">Clear List</button>
        </form>
    <% } else { %>
        <p>Your Want-to-Go List is empty! Start adding destinations from the homepage.</p>
    <% } %>

    <a href="/" class="btn btn-primary">Back to Home</a>
</body>
</html>
